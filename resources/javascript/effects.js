/*
* IN THIS VERSION IS NOT AVAILABLE
*/




/*
console.log("from effects.js");
const $ = require( 'jquery' );

$(document).ready(function(){

    //effectsWindow();;


});
function effectsWindow()
{

    const modalEffect = document.querySelector('.effectsBoard');
    const modalBtn = document.querySelector('#effectsMenu');
    const closeBtn = document.querySelector('.closeWindow');

    // Events
    modalBtn.addEventListener('click', openModal);
    closeBtn.addEventListener('click', closeModal);
    window.addEventListener('click', outsideClick);

    // Open
    function openModal() 
    {
    modalEffect.style.display = 'block';
    }

    // Close
    function closeModal() 
    {
    modalEffect.style.display = 'none';
    }

    // Close If Outside Click
    function outsideClick(e) 
    {
        if (e.target == modalEffect) 
        {
            modalEffect.style.display = 'none';
        }
    }
}

*/